import { memo } from 'react'
import { StyleSheet, Text, View } from 'react-native'

const ReadNews = () => {
    return (
        <View>
            <Text>ReadNews</Text>
        </View>
    )
}

export default memo(ReadNews)

const styles = StyleSheet.create({})