import { StyleSheet, Text, View } from 'react-native'

const UserInfo = () => {
    return (
        <View>
            <Text>UserInfo</Text>
        </View>
    )
}

export default UserInfo

const styles = StyleSheet.create({})