import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        // borderWidth: 1,
        flex: 1,
        alignItems: 'center',
    },
    number: {
        width: '100%',
        marginTop: 56,
    },
    button: {
        marginTop: 48,
        width: '100%',
        height: 48,
    }
})